{
  "name": "js-transformabit-demos",
  "version": "0.1.0",
  "description": "A sandbox for js-transformabit transformations.",
  "scripts": {
    "build": "cd .atomist ; npm run build",
    "demo": "cd .atomist ; npm run demo",
    "demo-watch": "cd .atomist ; npm run demo-watch"
  },
  "dependencies": {
    "@atomist/rug": "^0.10.0",
    "js-transformabit": "^0.4.0"
  },
  "devDependencies": {
    "@types/node": "^7.0.0"
  }
}
